#!/usr/bin/env bash

if [ ! -f ./docker-compose.custom.yml ]; then
    docker-compose -f docker-compose.yml -f docker-compose.dev.yml "$@"
else
    docker-compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.custom.yml "$@"
fi
